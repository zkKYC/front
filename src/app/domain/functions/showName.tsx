import { Container, Input, Button } from "@chakra-ui/react";
import React from "react";

const ShowName = () => {
  return <Container>Вывод картинки SBT + само имя</Container>;
};

export default ShowName;
